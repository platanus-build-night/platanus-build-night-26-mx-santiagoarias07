import ArchitectureDoc from "@/components/architecture";

export const metadata = {
  title: "Invariant — Architecture",
};

export default function ArchitecturePage() {
  return <ArchitectureDoc />;
}
